# SDD ledger — plan: docs/superpowers/plans/2026-08-01-phase2-dock-overlay.md
Task 2.0: controller ruling (amends locked contract): completion-hide claims ownership only on visible→hidden transition (hiddenByCompletion: s.hiddenByCompletion || s.overlayVisible) — reviewer-found hole, operator pre-hide never overridden
Task 2.0: fix round 1/5 (5 groups addressed, 0 open — ownership formula, completionHide idempotent no-op, endSession clears flag, 7 new tests, comments; commits fa3cca6..19026fc)
Task 2.0: minor (deferred): isSession accepts impossible {overlayVisible:true, hiddenByCompletion:true}; property suite has no hide-kind base; pre-Task-2.0 persisted sessions load as 'invalid' (accepted: nothing shipped)
Task 2.0: complete (commits 5e6ddf6..19026fc, review clean after fix round 1)
Task 2.1: minor (deferred): esbuild/vite transitive audit advisories (dev-server only) — revisit before Phase 4 sign-off
Task 2.1: complete (commits 19026fc..06b48aa, review clean)
Task 2.2: minor (test-artifact only): wrong-password test asserts clients()===0 after the existing 300ms real-wait, not immediately on auth-failed — mock's server-side socket bookkeeping can lag the client's own close event by a tick on a 4009 close; not a client bug
Task 2.2: fix round 1/1 (2 Important + 1 Minor addressed — request() now gates on state==='identified' not just readyState; added protected onBackoffScheduled/simulateClose/simulateIdentified seams + dedicated fake-timer test proving backoff growth/cap/reset at the real [1000,10000] default; interleaved-requests test now forces out-of-order responses via a new mock delayNextResponse hook; also corrected an inaccurate "no dom lib needed" claim in the report — lib.dom.d.ts is actually included by TS's default-for-target since tsconfig never narrows `lib`; commit 8c871ae..39b1458, 11/11 tests, 197/197 total, tsc clean)
Task 2.2: complete (commits 06b48aa..39b1458, review clean after fix round 1)
Task 2.2: fix round 1/5 (3 addressed, 0 open — backoff progression/cap/reset proven via protected seams on real code paths, request() gated on identified, out-of-order correlation test; commits 8c871ae..39b1458)
Task 2.2: minor (deferred): manual connect() during pending backoff leaves harmless dangling timer; EventListener local alias shadows DOM name
Task 2.2: complete (commits 06b48aa..39b1458, review clean after fix round 1)
Task 2.3: fix round 1/5 (2 addressed + tests, 0 open — guarded writes w/ onWriteError + memory log fallback, mirror routed through engine migration; commits b9681d3..962e195)
Task 2.3: minor (deferred): unguarded getItem paths (readLogRaw/loadSnapshot/loadSettings) — reads can theoretically throw; no test for storage-recovers-mid-session log catch-up; two same-source dock instances see each other as echoes (commented, single-operator design)
Task 2.3: complete (commits 39b1458..962e195, review clean after fix round 1)
Task 2.4: minor (deferred): dispatch() null-session synthetic result types session as Session but returns null — narrow the return union so consumers must check
Task 2.4: minor (deferred): heartbeat value repeats between beats by design — Task 2.7 overlay watchdog MUST key on message arrival, not heartbeat-value change (pin in 2.7 tests)
Task 2.4: minor (deferred): no dispose() — page-lifetime acceptable; revisit if 2.5 re-creates controllers
Task 2.4: minor (deferred): untested branches — holdThenHide callback still-complete guard, endSession-cancels-pending-hold, recovered:true for manual stored session
Task 2.4: judgment calls accepted: recovered=true for any loaded session; endSession keepOverlay degrades to cleared snapshot when style unknown (root fixed in 2.6 preset re-derivation)
Task 2.4: fix round 1/5 (5 Important + 6 minors addressed, 0 open — guarded notify after broadcast, onSleepGap test, init clobber guard, lastAction self-dispatch filter, load-warning logging, shared nonce helper, NonceWindow depollution; commits 51d9332..33ca4a0)
Task 2.4: complete (commits 962e195..33ca4a0, review clean after fix round 1)
Task 2.5: fix round 1/5 (2 Critical + 3 Important + 1 minor addressed, 0 open — controller.dispose() + boot teardown, focus-preserving render + targeted banner polls, ws-down/reload + banner-overlay coverage, sampling loop for pause-stability; commits f72ce4c..1d83f3d)
Task 2.5: accepted behavior change: banner-ws is a continuous 500ms connectivity poll (brief said one-shot first-run) — strictly more correct, verified not to re-render/steal focus
Task 2.5: minor (deferred): overlay-banner predicate duplicated between updateOverlayBanner and render
Task 2.5: complete (commits 33ca4a0..1d83f3d, review clean after fix round 1)
Task 2.6: bug found + fixed during first spec run (not a review-round finding): setup.ts's performSave() was unconditionally entering "editing" mode after every save, including brand-new ones — a subsequent setup-start-session on an unrelated ad hoc config would silently inherit the just-saved preset's id, corrupting recovery re-derivation. Fixed: only an update-of-an-existing-preset save stays in editing mode.
Task 2.6: judgment call: PRD §8.7 Restart-vs-Keep is implemented only for Presets' preset-start (brief's explicit scope) — Setup's own setup-start-session always replaces any active session immediately, same as SessionController.startSession()'s existing contract; documented inline.
Task 2.6: minor (deferred): setup-test-anim animates the whole setup-preview element regardless of setup-anim-target (number/text/both) — preview is one combined text node; per-part targeting belongs to the real overlay renderer (Task 2.7)
Task 2.6: minor (deferred): setup-preview only reflects the "style basics" exposed in this task's form; outline/shadow/background/alignment/fontWeight/textSizePx are fixed defaults in every built StyleConfig (no form control yet, out of this task's scope)
Task 2.6: fix round 1/1 (2 Critical + 1 Important addressed, 0 open — canSave/canStart gain rangeValid()+completionValid() gates, performSave() defends with isPreset before persisting, startSession wrapped in try/catch surfacing setup-error, presets.ts onDuplicate/onDelete re-fetch from storage before mutating; 4 new Playwright tests; commit 3b84e36)
Task 2.6: complete (commits 4b41c04..3b84e36, 270/270 vitest, 43/43 Playwright, typecheck+build clean, review clean after fix round 1)
Task 2.6: NOTE for Task 2.7 brief: preset-backed recovery emits two broadcasts (init null-style, then adoptPresentation) and keep-value emits two (pre-clamp, clamped) — overlay renderer must not eagerly paint a null-style frame when session.presetId is non-null; coalesce rapid broadcasts
Task 2.6: minor (deferred): stale-edit guard is millisecond-exact updatedAt equality — same-ms concurrent edits undetectable (acceptable for human timing)
Task 2.6: fix round 1/5 (2 Critical + 1 Important addressed, 0 open — save/start validation gates + isPreset defense, error surfacing, fresh-fetch mutations; commits 4b41c04..3b84e36)
Task 2.6: minor (deferred): duplicate/delete after a corrupt-quarantined load could overwrite with a near-empty list (pattern predates fix; latent); onDuplicate copies possibly-stale row field values (list preservation fixed, content staleness not)
Task 2.6: complete (commits 1d83f3d..3b84e36, review clean after fix round 1)
Scope addition (user, 2026-08-01): Task 2.9 preset export/import added to plan + PRD §8.7 + AC21 — runs after 2.8, before phase gate
Task 2.7: fix round 1/5 (4 Important attempted — 1/3/4 fixed; Imp-2 latch fix introduced verified reconnect default-style flash regression; commits e5b12d9..e3be762)
Task 2.7: fix round 2/5 (regression fixed via per-preset presentation cache — reconnect holds style with no flash/delay; hides never buffered; 3 new tests; commits e3be762..b8f4b75)
Task 2.7: minor (deferred): single-slot coalesce buffer could drop one frame if two never-seen presets race within 150ms (unrealistic single-session flow); presentationCache unbounded per page life (accepted); background.opacity unapplied (documented simplification)
Task 2.7: complete (commits 3b84e36..b8f4b75, review clean after fix round 2)
Task 2.8: bug found during GREEN pass (not a review-round finding): diagnosticsHandle.destroy() never cleared its container's DOM — this view builds once and never rebuilds, so a settings-save reconnect (destroy old, mount fresh into the same pane) left duplicate diag-row-*/settings-* nodes behind (caught by Playwright strict-mode locator). Fixed: destroy() now clears container.innerHTML.
Task 2.8: bug found during GREEN pass: MutationObserver.observe(document.documentElement, ...) inside a page.addInitScript() silently observes nothing (documentElement is still null at init-script-eval time) — fixed by observing `document` itself.
Task 2.8: added a second test seam (`refreshMs`, `?diagRefreshMs=`) alongside the existing `overlaySilenceMs` — a shrunk silence threshold is unobservable if the poll interval isn't shrunk to well below it too.
Task 2.8: judgment call: integration.spec.ts's 7 brief-numbered scenarios written as 7 separate self-contained tests (own mock server each), matching every other spec file's existing per-test-isolation convention, rather than one long sequential test.
Task 2.8: judgment call: one pre-existing test (live.spec.ts's settings-save-reconnect test) updated to click tab-diagnostics first — settings-port/settings-save moved out of the always-visible top row into the Diagnostics pane, per the brief.
Task 2.8: complete (commit 5036ef3, 283/283 vitest unchanged, 81/81 Playwright — 61 pre-existing + 13 diagnostics + 7 integration, tsc+build clean)
Task 2.8: review fix round 1/1 (1 Important + 4 Minor addressed, 0 open — hotkeys row now data-state='neutral' (distinct gray, never 'ok'), copy-diagnostics locks its exact "Hotkeys: not built yet (Phase 3)" line; overlay-URL round-trip test with a&b=c#d password through the REAL overlay page; probeStorage fail branch exercised via Storage.prototype.setItem monkey-patch; banner-ws gains role=button/tabindex=0/Enter+Space handler + keyboard test; diag-log's updateLog() now skips its rebuild when document.getSelection() intersects the log container, preserving an operator's in-progress selection across the 2s poll — 4 new tests, 1 test text updated; commit 39864d7)
Task 2.8: pre-existing flake class observed during re-verification (not introduced by this task): occasional single-test failures under 5-worker parallel `npx playwright test` (hit both a NEW test here and an unrelated PRE-EXISTING overlay.spec.ts test in different runs) — same documented, accepted resource-contention class from Task 2.7's report. Standalone spec runs and the large majority of full-suite runs are clean (9+ consecutive clean full-suite runs during this fix round); not chased further.
Task 2.8: fix round 1/5 (1 Important + 4 minors addressed, 0 open — neutral hotkeys row, password round-trip test, storage-fail test, banner keyboard access, log selection guard; commits 5036ef3..39864d7)
Task 2.8: minor (deferred): parallel-worker resource-contention flake class under 5-worker Playwright runs (documented in 2.7+2.8 reports; deterministic under --workers=1) — consider workers cap in playwright.config for the gate
Task 2.8: complete (commits b8f4b75..39864d7, review clean after fix round 1)
Task 2.9: fix round 1/5 (3 Important addressed, 0 open — idempotent numbered title uniqueness, export clipboard fallback textarea, full-field AC21 assertions; commits 7bd46e3..d00f697)
Task 2.9: complete (commits 39864d7..d00f697, review clean after fix round 1)
All 10 tasks complete — phase gate verification + final whole-branch review
Final review: 3-lens workflow → 16 confirmed majors deduped to 2 Critical + 11 Important + 3 folded minors; fix wave ad81e84 addressed 12/13; re-review found F5 residual (Case 1 reproduces) PLUS new Important breakage (mirrored tombstone deletes new session whose mirror write failed)
CONTROLLER DEVIATION (documented): dispatching a SECOND surgical fix round scoped only to F5+Case2 (monotonic revision seed via DockStorage.lastKnownRevision) instead of parking — a known introduced data-loss path must not be handed to the user checkpoint; if this round's re-review is not clean, STOP and surface per skill
Deferred to user checkpoint / Phase 3 (from re-review out-of-scope): cold-start identify can exceed 2.5s window (mirror unread — Phase 3 retry-on-first-identify); offline boot shows empty state ≤2.5s (Restoring… placeholder); chip SHOWING vs no-overlay-page contradiction (by ruling — Phase 3 ties chip to overlay presence); modifier chords count on visible Live tab; presets initial refresh pre-identify
F5 surgical round 2: ADDRESSED, no new Critical/Important breakage (monotonic revision lineage; commits ad81e84..5c8b664)
Deferred follow-ups (Phase 3): test the mirror-wins-then-start branch of observe; corrupt-quarantine drops lineage revision (Case-1-through-a-different-door, pre-existing); Number.isSafeInteger guard in noteRevision; init-window re-stamp (~3 lines in clobber-guard branch)
Phase 2 COMPLETE: 297 vitest + 104 Playwright + typecheck + build green; final review clean after fix wave + surgical round
Task 2.10: complete pending fix round 1 (stale paste-error hints) — commits 08417d1..e2fb229
SPIKE RESULT (2026-08-02, setup-friction): in Chromium, two file:// pages share localStorage, receive cross-page `storage` events, AND BroadcastChannel works between them. => dock<->overlay bus does NOT require obs-websocket. Plan: dual-transport Bus (BroadcastChannel primary + existing obs-websocket fallback) makes the websocket password OPTIONAL for counting; websocket then only unlocks auto-add-overlay + richer LIVE status. NEEDS CEF-127 confirmation in real OBS (fold into next smoke test; Diagnostics should display which transport is live).
Scope queue before merge: 2.10 fix -> 2.11 layout gallery -> 2.12 setup buttons (add-overlay + connect card) -> 2.13 dual-transport bus (pending CEF confirmation)
Task 2.10: fix round 1/5 (1 Important + 1 minor addressed, 0 open — paste-error cleared on apply/save and on manual field edit; import-paste now dispatches real input event; commits e2fb229..578f87c)
Task 2.10: complete (commits 08417d1..578f87c, review clean after fix round 1)
Task 2.10: minor (deferred): integration.spec automatic-run reload test flakes under 5-worker parallelism (pre-existing class, clean at --workers=1)
Task 2.11: fix round 1/5 (2 Important + 5 minors addressed, 0 open — behindWrapEl carries centring so ghost animates; {count} requirement dropped, inlineContent() decides placement by layout with token-compat path; commits 246b323..40fd78e)
Task 2.11: CONTROLLER SPEC ERROR corrected mid-task (my textBefore/textAfter both split on token = identical rendering) — PRD 8.8 + AC22 rewritten, commit 56680a8
Task 2.11: complete (commits 578f87c..40fd78e, review clean after fix round 1)
Task 2.12: fix round 1/5 (1 Critical + 1 Important + 3 minors addressed, 0 open — single coalescing add-overlay lock w/ busy notification, destroyed-guard + container clear in live destroy, mock merge semantics, shared grace constant, per-input scan try/catch; commits 77b5b5a..022a540; reviewer reproduced both bugs via negative control)
Task 2.12: complete (commits 40fd78e..022a540, review clean after fix round 1)
Task 2.13: complete (commits 022a540..04e882d, review APPROVED). Two Important items deferred BY REVIEWER RECOMMENDATION into the phase-gate fix wave: (a) LocalBusTransport.destroy() leaves the localStorage half live — a destroyed bus can still write/report success (zombie-writer class); (b) Bus.send() uses allSettled so an identified-but-unresponsive OBS blocks broadcast for the full 8s request timeout even though local already delivered — should resolve on first success. Also for the wave: transport-row availability is capability-only (can show green while writes fail); no test for warn/fail transport row states; no-params overlay no longer falls back to default ws port (sign off at gate); consider revision-monotonic guard on applied state; document the file://-shared-origin trust note for the local bus.
ALL PHASE 2 BUILD TASKS COMPLETE (2.0-2.13). Phase gate: verify suites -> final review of delta since last whole-branch review (d00f697..HEAD) -> fix wave -> real-OBS smoke with user -> merge decision.
GATE FIX WAVE complete (commit 2aee546): 4 confirmed majors + 13 minors addressed; scoped re-review = ALL ADDRESSED, no new Critical/Important. Two new Minors introduced, deferred to Phase 3 + smoke list: (1) staleness re-check compares scan.status but not the confirmed input/scene identity (two-overlay or scene-switch-mid-confirm races); (2) group/nested-scene membership invisible to GetSceneItemList → an overlay inside a group reads as other-scene and attaching adds a duplicate top-level item.
SMOKE-CHECKLIST ADDITIONS from the re-review: verify CreateSceneItem attach path in real OBS; confirm CEF-127 supports the local transport (BroadcastChannel/localStorage across file:// pages) — Ruling B means a default-port created source has NO ws fallback and depends on it; Fix path silently swaps a hand-added credentialed URL for the password-free one (confirm text should say so — Phase 3).
PHASE 2 COMPLETE: 337 vitest + 169 Playwright + typecheck + build green.
Task 2.14: fix round 1/5 (3 Important + 6 minors addressed, 0 open — dispose-before-clear reset ordering (negative-control verified), preview scaled instead of word-break, parity driven from DEFAULT_STYLE + cross-page computed-style assertion, storage seam for clear, template-example removed, Test animation moved to Animation group; commits c1c0224..f783e8b)
Task 2.14: complete (commits 2aee546..f783e8b, review clean after fix round 1). overlay.spec.ts unmodified throughout the extraction — the regression fence held.
Deferred (Phase 3): settings-save during the reset await window can rewrite lc.settings.v1 (pre-existing, manual-action edge case); stale add-overlay intent identity; group/nested-scene membership blind spot; multi-overlay warning (L7); glyph-coverage warning (PRD 8.8) unimplemented.
PHASE 2 COMPLETE (incl. operator-feedback rounds 2.10-2.14): 344 vitest + 179 Playwright + typecheck + build green.
Task 2.15: complete (commits f783e8b..32797a4, review APPROVED first pass — reviewer verified all 3 new tests RED against pre-fix code in a worktree). Sticky tabs + captioned sticky preview + inline label vertical centring (shared module => overlay and preview together). Two incidental root-cause fixes: body was a dead scroll container via overflow-x coupling (defeating sticky, and making the 300px no-horizontal-scroll assertions near-tautological — they are now meaningful and still pass); Setup preview locked at 0px when first rendered in a hidden pane (ResizeObserver fix).
Minors (Phase 3): tabs container derived via parentElement rather than its own testid; no test for tab labels wrapping to two rows.
PHASE 2 COMPLETE incl. all operator-feedback rounds: 344 vitest + 182 Playwright + typecheck + build green.
Task 2.16: complete (commits 32797a4..7ad151c, review APPROVED first pass — reviewer re-verified RED in a clean worktree). animationTargets() extracted to the shared module; Setup test-anim now animates exactly the targeted nodes. Additive overlay behaviour: numberOnly+target'text' animates the counter (no prior assertion covered it).
Scope queue (operator feedback, in order): 2.17 literal label whitespace, then 2.18 update-a-running-session (engine `reconfigure` command).
Task 2.17: complete (commits 7ad151c..88c59be, review APPROVED first pass — reviewer RED-verified all 7 new tests against base in an isolated worktree). TWO root causes: setup.ts trimmed the stored/painted template at 3 sites, AND label nodes lacked whitespace-preserving CSS. Note: all-whitespace label still saves as null (pre-existing, untested).
Minors (Phase 3): 3 repeated whiteSpace re-asserts in renderPreviewBlock could be a loop; no test for all-whitespace-label-as-null; Start-session path not covered by a space-preservation test.
Task 2.18: complete (commits 88c59be..6b8f907, review pending). New engine 'reconfigure' command (validation mirrors createSession; clamps currentValue; clears undoStack; never enters complete, only ever exits via exitComplete reuse). Controller re-arms AutoTimer via setIntervalSeconds on interval change, no restart. Setup gets prefill-from-active-session (mount + tab-activation, guarded against clobbering an in-progress preset edit) and an Update session button (adoptPresentation + reconfigure, clamp warning, switches to Live). 370 vitest (344+26) + 201 Playwright (195+6) + typecheck + build green.
Bug found but NOT fixed here (out of scope, flagged via spawn_task): setup.ts performSave() reads ui.startValue/finishValue/style/etc AFTER its await opts.storage.loadPresets() call, not captured before it -- a fast re-edit of the same fields before that async round-trip resolves silently saves the LATER values instead of what was on screen at Save-click time.
Task 2.18: fix wave 1/1 (3 Important + 6 minors addressed, 0 open — direction only carries over within the same range orientation (fix wave 1, snaps to new orientation on flip, closing the 0->50-into-50->0 completion-at-wrong-end bug); refresh() gated by a new dirty flag so unsaved Setup edits survive a tab round-trip; Update now dispatches setMode before reconfigure so a mode change actually takes effect; true no-op on an identical reconfigure; completion shallow-copied; reconfigure dispatches before adoptPresentation; clamp warning uses banner-warn styling; corrected two exclusion-comment justifications and added reconfigure to the property suite's cmdArb fuzzing; commits 6b8f907..652c4d9).
Task 2.18: complete (commits 88c59be..652c4d9, review fix wave 1 applied). 376 vitest (344+32) + 205 Playwright (195+10) + typecheck + build green.
Task 2.18: fix wave 2/2 (re-review) — 1 Important + 2 minors addressed, 0 open. The whole-form dirty boolean from fix wave 1 interacted with the mode-dispatch fix into a new bug: editing ANY field suppressed refresh() entirely, so Mode/Interval froze at stale values and Update could silently setMode a running automatic session back to manual (or revert a Faster/Slower). Replaced with per-field dirtyFields: Set<string> — refresh() re-prefills every clean field, reconfigureCommandFor()/resolvedModeFor() resolve the Update payload per field (operator value if dirty, live session value otherwise). canUpdate() now pre-validates the exact payload via a direct (side-effect-free) applyCommand call, closing an intervalSeconds-not-in-SPEED_LEVELS gap and making the reject-leaves-mode-applied minor unreachable through the UI. Corrected a timing-fragile literal assertion and the round-1 fix report's overstated fuzz-depth claim (fast-check size bias means ~5 commands/run, not 400). commits 652c4d9..14466bf.
Task 2.18: complete (commits 88c59be..14466bf, review fix waves 1+2 applied). 376 vitest + 209 Playwright (205+4) + typecheck + build green.
Task 2.18: fix wave 3/3 (re-review) — 1 Important + 2 minors addressed, 0 open, plus 1 coordinator-requested confirming test. loadPreset()/performSave() were both clearing dirtyFields (fix waves 1+2 carried this over from the old whole-form model), which combined with per-field resolution meant Update silently applied the SESSION's own stale values instead of a just-loaded-preset's or just-typed-then-saved value -- no error, no cue. Ruling: only Start/Update/prefillFromSession may clear; loadPreset() now marks its written fields dirty (authored values awaiting Update); performSave() leaves dirtyFields untouched entirely. Also: canUpdate() now also requires completionValid() (agreeing with Save/Start on integer hold-seconds, not just the engine's more-permissive isCompletionConfig); an off-menu recovered interval now substitutes the nearest SPEED_LEVEL instead of permanently disabling Update (rewrote the fix-wave-2 test whose expectation this inverts). Confirming test requested by the coordinator found a label-only Update does NOT hit the engine's noop path when undo history exists (reconfigure's noop condition requires an empty undo stack too, a deliberate fix-wave-1 rule) -- reported, not silently patched into the engine. commits 14466bf..58d6788.
Task 2.18: complete (commits 88c59be..58d6788, review fix waves 1+2+3 applied). 376 vitest + 214 Playwright (209+5, 1 rewritten) + typecheck + build green.
Task 2.18: fix wave 4/4 (re-review) — STRUCTURAL, not another patch: rounds 2/3 each hit a genuine new regression, and each fix was faithful to its own ruling, but all three were the SAME underlying class (a stale form value silently mutating a live session). This round deletes three mechanisms instead of layering a fourth: resolvedModeFor() and every setMode dispatch from onUpdateSession() (Setup never changes mode now -- Mode select disabled + shows the live session's mode while active, mismatch note explains a loaded preset's differing mode); nearestSpeedLevel() substitution (engine's reconfigure now accepts an intervalSeconds equal to the session's own current value regardless of SPEED_LEVELS membership -- rule 5); and the s.undoStack.length===0 clause in reconfigure's noop check (undo now clears only when startValue/finishValue actually change; an identical payload is always a true noop regardless of undo history). Ruling 4 adds a persistent 'not applied yet' notice when the displayed form differs from the running session. Negative-control verified the off-menu-interval regression test (reverted the fix locally, confirmed the test fails with 1.5 instead of 1.3, restored). commits 58d6788..41ada97.
Task 2.18: complete (commits 88c59be..41ada97, review fix waves 1+2+3+4 applied -- fix wave 4 structural). 383 vitest (376+7) + 219 Playwright (214+5, several rewritten) + typecheck + build green.
Task 2.18: fix wave 5/5 (re-review, FINAL) — rulings 1-3 from fix wave 4 verified fully structural, no new breakage; ONE field escaped that redesign (presentation) and carried the worst instance of the class: on-air, silent, mid-service. onUpdateSession pushed buildStyle()/template/buildAnimation() unconditionally with no way to learn what was actually live, so a dock restart of a preset-backed session (init -> adoptPresentation restores the on-air look, but Setup's form holds defaults) meant ANY Update -- even a pure range bump -- silently stripped the overlay to a default white number mid-service. Fix: ControllerState gains presentation:{style,template,animation}|null (additive, pre-authorized); prefillFromSession/onUpdateSession/formDiffersFromSession give presentation the SAME per-field dirty-aware treatment session fields already have (resolvePresentation/presentationCommandFor mirror reconfigureCommandFor); null presentation (deleted preset) -> form keeps defaults, notice always shows, Update applies exactly what's displayed. Residuals: loadPreset no longer marks intervalSeconds dirty when the live session is manual (invisible field); Live-tab mode-toggle-stops-timer test added (replaces deleted Setup coverage); 1.3 added to cmdArb's interval fuzzing; double blank line removed. Two negative controls performed and confirmed (off-menu interval unchanged from wave 4; reviewer's exact repro genuinely needs BOTH the prefill and resolve halves -- reverting just one still passed, revealing the two are complementary not redundant). commits 41ada97..965b4b3.
Task 2.18: COMPLETE (commits 88c59be..965b4b3, review fix waves 1-5 applied, coordinator confirmed this closes the task). 386 vitest (383+3) + 225 Playwright (214+11, several rewritten) + typecheck + build green.
Task 2.18: fix rounds 1-5 (commits 6b8f907..965b4b3). Round-by-round: engine correct from round 1 (adversarially traced); the Setup form's which-values-did-the-operator-mean bookkeeping regressed 4 times (whole-form dirty flag -> stale mode stops running session -> per-field tracking -> preset-load applies nothing -> clearing-points rule -> presentation escaped the redesign entirely). Round 4 was structural (Setup never sets mode; unchanged interval always valid; undo cleared only on range change; staleness notice). Round 5 gave presentation the same treatment (ControllerState.presentation, dirty-aware prefill/resolve/apply/notice, null-presentation fallback). Final re-review: ALL CLEAR, nothing Critical/Important open.
Task 2.18: parked (ruling): seven non-operator-editable StyleConfig fields (outline/shadow/background/fontWeight/align/padding) flattened by Update/Save when an IMPORTED preset carries them — Setup never modelled them; pre-existing; Phase 3 fix = spread live style and override only modelled fields in resolvePresentation/buildStyle
Task 2.18: parked (ruling): out-of-bounds imported size disables Update until the visible field is corrected (new, minor, recoverable; same Phase 3 fix)
Task 2.18: minors (deferred): cross-window presentation comment overstates a guarantee; cmdArb 1.3 draw only exercises the reject path; boot-window display staleness (form lies, applies correctly) pre-existing
Task 2.18: complete (commits 88c59be..965b4b3, review clean after 5 rounds). LESSON (recorded): reconciling three sources of truth in a form (live session / operator edits / loaded preset) is a design problem, not a bookkeeping problem — solve by removing writers (mode) and making unchanged-is-valid (interval), not by tracking cleverness.
FINAL GATE (2.14-2.18 delta): 3-lens workflow → 6 confirmed majors deduped to 4 (ghost preview clipping w/ deeper pre-transform-clip root cause; clamp warning orphaned by navigation; Updated presentation not surviving reload; doc drift). Docs fixed by controller (commit before wave); fix wave c8dfe76 addressed everything + 9 minors; re-review: ALL ADDRESSED, no new Critical/Important.
Parked (rulings, Phase 3 list): rejected-Update stale clamp banner (unreachable in practice per canUpdate gate); hold re-arm wider than U4's letter (over-holds, matches init precedent); F3 '(current)' label stale after session end; scale-box tests key on CSS class; createSession throw surfaces raw engine message on off-menu-interval Start (pre-existing); performSave writes ui.mode while control shows session mode (preset keeps own mode — arguably correct, undisplayed); lc.presentation.v1 silently discarded on corruption (deliberate: pure cache); stored presentation not session-keyed (cross-machine mirror gap, by ruling ordering); preset-deleted-but-stored-presentation-present combination untested (path covered via intact-preset test).
PHASE 2 COMPLETE AND GATED: 403 vitest + 234 Playwright + typecheck + build green at c8dfe76.
Task 2.19: complete (commits c8dfe76..758ef1a, review clean after fix round 1 — I-1..I-5 interception edges + 11 minors). Keyboard clipboard (Cmd/Ctrl+C/X/V/A) in all editable fields via once-installed document handler w/ execCommand insertText for caret+undo; password copy/cut refused with hint; Save always enabled w/ click-validation + title error state + focus; save confirmation "saved/updated ✓". SMOKE ADDITION: verify clipboard permissions in real OBS CEF (readText/writeText/execCommand) — automated tests cannot prove the real permission model.
PHASE 2 FINAL: 403 vitest + 256 Playwright + typecheck + build green at 758ef1a, 56 commits, tree clean.
